
import { useHistory } from "react-router-dom";

function GerrarLogin() {

  const history = useHistory()

  console.log(history)
    return (
        <p>login</p>
    )
}

export default GerrarLogin;
